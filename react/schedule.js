import React from 'react';
import { render } from 'react-dom';
import Appointments from '../react/components/Appointments.jsx';

render (
  <Appointments />,
  document.getElementById('appointments')
)